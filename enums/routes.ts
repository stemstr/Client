export enum Route {
  Home = "/",
  Discover = "/discover",
  Login = "/login",
  Signup = "/signup",
  Settings = "/settings",
  SettingsNsec = `${Settings}/nsec`,
  Thread = "/thread",
  Notifications = "/notifications",
  User = "/user",
  EditProfile = "/user/edit",
  Profile = "/profile",
}
